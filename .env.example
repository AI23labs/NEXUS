# NEXUS — Copy to .env at repo root and fill in values. No defaults for API keys.

# Mode: live | mock_ai | mock_human
NEXUS_MODE=mock_ai

# Data stores. For Docker: use hostnames nexus-db and nexus-redis (see docker-compose).
# Example (local): postgresql+asyncpg://nexus:nexus@localhost:5432/nexus
DATABASE_URL=postgresql+asyncpg://nexus:nexus@nexus-db:5432/nexus
REDIS_URL=redis://nexus-redis:6379/0

# Twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ElevenLabs (agent_id = Conversational AI agent for WebSocket; distinct from voice_id)
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=
ELEVENLABS_VOICE_ID=

# OpenAI
OPENAI_API_KEY=

# Google OAuth (multi-user; no service account)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=https://api.example.com/api/auth/callback

# Session & encryption (production)
SESSION_SECRET_KEY=  # min 32 chars for HMAC; e.g. openssl rand -hex 32
ENCRYPTION_KEY=  # Fernet key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# SESSION_COOKIE_SECURE=true  # set false for localhost (http) so session cookie is sent

# Google APIs (Places + Distance Matrix; optional, fallback to mock providers)
# Config reads GOOGLE_API_KEY only (use this name in .env).
# GOOGLE_API_KEY=

# ElevenLabs outbound (Twilio) — required for real calls. Get from ElevenLabs: Conversational AI → Phone Numbers (create/link Twilio number); copy the phone number ID into .env. If unset, campaigns run but no call is placed (check logs for elevenlabs_outbound_skipped).
# ELEVENLABS_AGENT_PHONE_NUMBER_ID=

# Optional: MOCK_HUMAN mode. For MULTIPLE numbers (swarm to 3 people): set ONLY TARGET_PHONE_NUMBERS (no spaces around commas). For one number: set TARGET_PHONE_NUMBER.
# TARGET_PHONE_NUMBER=
# TARGET_PHONE_NUMBERS=+16175551111,+16175552222,+16175553333
# MOCK_HUMAN_MAX_CALLS=3

# ElevenLabs webhooks: required only when using Docker ngrok (--profile expose). Get token: https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_AUTHTOKEN=
