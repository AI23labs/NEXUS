# NEXUS — Production Configuration

# --------------------------------------------------------
# 1. CORE SETTINGS
# --------------------------------------------------------
# MUST be 'mock_human' for the phone test. 
# 'mock_ai' skips Twilio entirely.
NEXUS_MODE=mock_human

# --------------------------------------------------------
# 2. DOCKER NETWORKING (CRITICAL FIX)
# --------------------------------------------------------
# We use service names 'nexus-db' and 'nexus-redis', not localhost.
DATABASE_URL=postgresql+asyncpg://nexus:nexus@nexus-db:5432/nexus
REDIS_URL=redis://nexus-redis:6379/0

# --------------------------------------------------------
# 3. TWILIO (The Ears)
# --------------------------------------------------------
TWILIO_ACCOUNT_SID=       # Paste actual SID
TWILIO_AUTH_TOKEN=      # Paste actual Token
TWILIO_PHONE_NUMBER=       # Your purchased Twilio number

# TWILIO_ACCOUNT_SID=""       # Paste actual SID
# TWILIO_AUTH_TOKEN=  ""          # Paste actual Token
# TWILIO_PHONE_NUMBER=+1...  
# --------------------------------------------------------
# 4. ELEVENLABS (The Mouth)
# --------------------------------------------------------
ELEVENLABS_API_KEY=       # Paste API Key
ELEVENLABS_VOICE_ID=       # Voice ID (e.g., JBFqnCBsd6RMkjVDRZzb)

# [NEW] AGENT ID (The Brain Wiring)
# Get this from ElevenLabs Dashboard -> Conversational AI -> Agents
ELEVENLABS_AGENT_ID=      

# --------------------------------------------------------
# 5. OPENAI (The Brain)
# --------------------------------------------------------
OPENAI_API_KEY=

# --------------------------------------------------------
# 6. GOOGLE (The Data)
# --------------------------------------------------------
# Google Maps Platform (The AI's Vision)
# GOOGLE_API_KEY=your_restricted_api_key_here

# Google OAuth2 (Multi-User Calendar Access)
# GOOGLE_CLIENT_ID=your_client_id_from_client_secret_json
GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=your_client_secret_from_client_secret_json
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/callback
# --------------------------------------------------------
# 7. SIMULATION TARGET (The Safety Net)
# --------------------------------------------------------
# CRITICAL: This is YOUR cell phone number. 
# In 'mock_human' mode, the AI calls THIS number, not the dentist.
TARGET_PHONE_NUMBER=     # E.164 format (e.g., +15550001234)
# TARGET_PHONE_NUMBERS=+16,

GOOGLE_API_KEY=


NGROK_AUTHTOKEN=
ELEVENLABS_AGENT_PHONE_NUMBER_ID=

# --------------------------------------------------------
# 8. FRONTEND (SPA)
# --------------------------------------------------------
# After Google login, redirect here. Also used for CORS.
FRONTEND_ORIGIN=http://localhost:5173
# OAuth callback must hit the FRONTEND URL so the session cookie is set for 5173 (otherwise cookie is for 8000 and won't be sent from the app).
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:5173/api/auth/callback